// We don't support if statements without braces, so normal elseif statements
// won't work at the moment


int main(){
    int x;

    x = 10;

    if (x == 5){
        return 0;
    } else {if (x == 8){
        return 1;
    } else {if (x == 10){
        return 2;
    else{
        return 3;
    }
    }}
}
